{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      { "src": "/api/(.*)", "dest": "/api/:1" }
    ],
    "functions": {
      "api/*.js": {
        "runtime": "nodejs14.x"
      }
    }
  }
  
